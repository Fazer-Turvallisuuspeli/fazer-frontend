import React from 'react';

import { StyledGameContainer } from './GameContainerStyles';
import Landing from '../pages/Landing';

const GameContainer = () => {
  return (
    <StyledGameContainer>
      <Landing />
    </StyledGameContainer>
  );
};

export default GameContainer;
